export declare class ClientService {
    validatePassword(username: string, password: string): {
        id: number;
        name: string;
        username: string;
        password: string;
    };
}
